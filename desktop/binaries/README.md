The build script writes `gridbot-backend-<native-Rust-target-triple>` here.
These are real PyInstaller native executables. They are generated on the target
macOS architecture and intentionally are not replaced with script placeholders.
